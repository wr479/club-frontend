const titles = document.querySelectorAll('.accum-info > h1');
const pages = document.querySelectorAll('.sortpages-page');

pages.forEach(function(e) {
    if (e.className == 'sortpages-page for-order') {
        
    };
}) 
console.log(pages);